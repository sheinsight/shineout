export const changeSubscribe = name => `change-subscribe-${name}`

export const errorSubscribe = name => `error-subscribe-${name}`

export const updateSubscribe = name => `update-subscribe-${name}`

export const FORCE_PASS = {}

export const ERROR_TYPE = {}
